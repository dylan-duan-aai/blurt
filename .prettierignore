# Build / cache directories
.build/
build/
.swiftpm/
.superpowers/
# Per-developer agent scratch notes (git-ignored, so CI never sees them — but a
# local `prettier --check` globs them and fails the run before it reaches the
# real checks).
.remember/
**/.DS_Store

# Xcode-generated files
*.xcodeproj/
*.xcworkspace/
Package.resolved

# Working docs / plans / marketing drafts — prose, not shipped source. Not linted.
docs/
